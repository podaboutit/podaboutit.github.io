// Declare variables (var) and assign them values in the same line.
var myNickname = "Doug Funny";
var myName = myNickname;
// Use let to assign items that won't change
let myStatus = " is awesome!";
console.log (myName + myStatus)